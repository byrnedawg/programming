% Program P2_9
% Generate the input sequence
clf;
n = 0:299;
x1 = cos(pi*n/2);
x2 = sin(pi*n/8);

% Compute the output sequences

% Plot the output sequences

plot(n,x1);axis([0 300 -2 2]);
ylabel('Amplitude');
